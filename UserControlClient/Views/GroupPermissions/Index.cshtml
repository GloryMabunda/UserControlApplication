@model IEnumerable<UserControlAPI.Models.GroupPermissions>

@{
    ViewData["Title"] = "Index";
}

<h1>Group Permissions Listing</h1>

@* <p>
    <a asp-action="Create">Create New</a>
</p> *@
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.GroupPermissionId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GroupPermissionName)
            </th>
            @* <th></th> *@
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.GroupPermissionId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GroupPermissionName)
            </td>
       @*      <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td> *@
        </tr>
}
    </tbody>
</table>
